The main logic is written in a file called geneticMusic.py.
Used pyknon and timidity, you can download and use those libararies as:
# sudo pip install pyknon
# timidity -Or -o - first.mid | lame -r - first.mp3 (convert)
# for running #timidity -T 350 demo.mid


